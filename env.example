# Get a free key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Default is fast and cheap on free tier; change if Google renames models
GEMINI_MODEL=gemini-2.0-flash

# Optional: use OpenAI instead of Gemini (set only one primary; Gemini is checked first)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# CHAT_PROXY_PORT=8787
